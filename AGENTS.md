# 粗精煤泥模型维护范围

- 用户已要求：后续只优化 GPT 版本，DS 版本保持不变。
- 不修改 DS 的训练、预测、日级聚合、参数选择与页面分支；DS 对拍基线 `backend/data/train_js.json` 不得随 GPT 优化重新生成。
- 用户进一步要求：GPT 必须独立设计，DS 仅作数据与效果比较参考；不以复刻 DS 的训练、聚合或选参流程作为优化方案。
- GPT 的试验和改动放在专用路径；保持 DS 依赖的共享求解器、特征定义及 DS 保存模型不变，并运行 DS 原版对拍。
- GPT 的历史拟合、调参验证、向前时间检验分别报告。已经用于开发的数据不作为新的独立检验数据。
- 现场已确认粗精煤泥化验当天出结果；历史记录尚无具体出报告时间，滞后化验试验不能默认同日较早采样的结果已可用。
