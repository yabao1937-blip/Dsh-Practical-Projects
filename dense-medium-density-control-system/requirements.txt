# 重介密控系统 - 第一步：预测模型 (XGBoost + LightGBM)
# 说明：xgboost / lightgbm 在 Windows 下默认安装为 CPU 版，GPU 版需源码编译；
#       代码已预留 device / device_type 配置，当前按 CPU 跑通。
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
xgboost>=2.0
lightgbm>=4.3
pyyaml>=6.0
joblib>=1.3

# --- 后续步骤（TCN 时序模型）再启用，CUDA 安装命令见 PyTorch 官网 ---
# torch>=2.2
