# -*- coding: utf-8 -*-
"""生成 Excel 文档：后续优化待办计划（含原因）。

产出 docs/待办-后续优化计划.xlsx，四个 Sheet：
总览与建议批次 / 待办清单(含原因) / 待你决策的问题 / 暂缓项与原因

范围：2026-09-10 完成「glm-5.3 改动审查 + 7 项修复」（提交 dff835b）之后
剩余的待办。每项均附「原因」——即为什么必须做/为什么这样做，
原因是判断优先级与取舍的依据，不是复述做法。
清单只包含已由实测或代码证据确认的问题，不含推测项。
"""
import json
from pathlib import Path

from openpyxl import Workbook
from openpyxl.styles import Alignment, Border, Font, PatternFill, Side

DOCS = Path(__file__).resolve().parent.parent.parent / "docs"
OUT = DOCS / "待办-后续优化计划.xlsx"

HEAD_FILL = PatternFill("solid", fgColor="2F5597")
HEAD_FONT = Font(name="微软雅黑", size=11, bold=True, color="FFFFFF")
TITLE_FONT = Font(name="微软雅黑", size=13, bold=True, color="2F5597")
BODY_FONT = Font(name="微软雅黑", size=10)
HIGH_FILL = PatternFill("solid", fgColor="F8CBAD")
MID_FILL = PatternFill("solid", fgColor="FFE699")
LOW_FILL = PatternFill("solid", fgColor="E2EFDA")
ASK_FILL = PatternFill("solid", fgColor="DDEBF7")
HOLD_FILL = PatternFill("solid", fgColor="EDEDED")
OK_FILL = PatternFill("solid", fgColor="DDEBF7")     # 已完成/已决
THIN = Side(style="thin", color="B0B0B0")
BORDER = Border(left=THIN, right=THIN, top=THIN, bottom=THIN)
WRAP = Alignment(wrap_text=True, vertical="top")
CENTER = Alignment(horizontal="center", vertical="top", wrap_text=True)


def sheet(wb, name, title, headers, widths, rows, fill_col=None, fill_map=None):
    ws = wb.create_sheet(name)
    ws["A1"] = title
    ws["A1"].font = TITLE_FONT
    ws.merge_cells(start_row=1, start_column=1, end_row=1, end_column=len(headers))
    for c, (h, w) in enumerate(zip(headers, widths), start=1):
        cell = ws.cell(row=3, column=c, value=h)
        cell.fill = HEAD_FILL
        cell.font = HEAD_FONT
        cell.alignment = CENTER
        cell.border = BORDER
        ws.column_dimensions[cell.column_letter].width = w
    for i, row in enumerate(rows, start=4):
        for c, v in enumerate(row, start=1):
            cell = ws.cell(row=i, column=c, value=v)
            cell.font = BODY_FONT
            cell.alignment = CENTER if c <= 2 else WRAP
            cell.border = BORDER
        if fill_col and fill_map:
            f = fill_map.get(str(row[fill_col - 1]))
            if f:
                for c in range(1, len(headers) + 1):
                    ws.cell(row=i, column=c).fill = f
    ws.freeze_panes = "A4"
    return ws


wb = Workbook()
wb.remove(wb.active)

# ------------------------------------------------------------------ 1 总览
sheet(
    wb, "总览与建议批次",
    "后续优化待办：总览与建议执行批次",
    ["项目", "内容"],
    [20, 132],
    [
        ["已完成（基线）", "提交 dff835b：glm-5.3 改动审查 + 7 项修复（F-1 分布图刻度、I-2 影响值口径统一、"
                       "F-2 简报文案与 0 行分诊、F-3 7月清理改一次性迁移、I-3 层级文案、B-1 决策日志响应改人工采样、"
                       "I-5/6/10 日期解析两侧同步）。pytest 59→65；真实库简报对拍 4208 格零差异；浏览器 A/B 与冒烟全通过。"],
        ["本表范围", "基线之后剩余的全部待办：8 项可直接执行、8 项需你决策、4 项建议暂缓。"
                   "每项都写明「原因」，原因是判断优先级和取舍的依据。"],
        ["待办分布", "可直接执行 10 项：缺陷修复 2 项 · 健壮性 3 项 · 文档 1 项 · 测试基建 1 项 · 数据治理 2 项 · 交付 1 项；"
                   "需决策 8 项；建议暂缓 4 项。"],
        ["推荐批次 1（约 1.5h，零口径风险）", "T-1 浮精灰分解析失败不再当 0 · T-2 xlsx-lite 补 inlineStr/十六进制引用 · "
                                        "T-3 更新 frontend/AGENTS.md · T-4 补导入跨语言 golden。"
                                        "理由：两项是「静默产生假数据」的缺陷，两项是防后续踩坑的基建，改动都不触及算法与口径。"],
        ["推荐批次 2（约 2h）", "T-5 年份常量化与跨年 · T-6 防回退守卫覆盖面 · T-7 存量影响值重算 · T-8 决策日志导出。"
                          "理由：都依赖批次 1 的测试基建来兜底；T-7/T-8 直接影响现场能不能用这些数据。"],
        ["推荐批次 3（需先决策）", "T-9 自定义日期格式对拍 · T-10 两个「新旧」判据统一。"
                            "理由：T-10 的修法取决于「是否引入 revision」这个架构选择，先定方向再动手。"],
        ["最该优先的一件", "T-1：浮精灰分单元格为空白或写「-」「？」时，解析结果是 0%，并且作为一条真实记录入库，"
                       "进而被 resolve_float_ash 取为最新值直接进入总灰分公式 —— 它会无声地拉低总灰分并影响密度建议，"
                       "而现场从界面上看不出来。"],
        ["流程提醒", "① 12 个提交已推送至 GitHub(2026-09-10,84c6924..d87d06e),远端与本地一致;"
                   "② 有另一个 agent（glm-5.3）在本仓库并发提交过,已把「同一时间只允许一个 agent 持有未提交改动」"
                   "写进 frontend/AGENTS.md 作为硬规则。"],
        ["本轮已完成(2026-09-10)", "批次0:核实 git → 决策 Q-1/Q-2 → 推送 12 个提交(未推送数归零)。"
                            "批次1:T-1 浮精/粗精灰分解析失败不再当 0、T-2 xlsx-lite 补 inlineStr 与十六进制引用。"
                            "批次2:T-3 更新 frontend/AGENTS.md、T-4 导入跨语言 golden。"
                            "批次3(本轮):T-5 年份跨年、T-6 守卫按类别细分、T-7 存量影响值重算、"
                            "T-8 决策日志导出、T-9 自定义日期格式、T-10 判据统一、T-14 errors 结构统一。"
                            "**T-1~T-10、T-14 全部完成**，pytest 59 → 71，详见「本轮已完成」表。"],
        ["本轮已完成(2026-09-11)", "同步链路（浏览器→服务器镜像）专项：M-1 守卫误拒（自 ca08e85 起同步实际已全死）、"
                            "M-2 keepalive 64 KiB 上限、M-3 镜像洗掉服务器化验样 三个缺陷修复；"
                            "M-4 写路径禁止静默吞错（结果返回 + 待发副本 + 守卫拒绝/传输失败两类分开处置）；"
                            "M-5 端到端脚本进 CI（用例0 冷启动写不得被拒 + 失败不静默 + 重试送达，"
                            "另含密度决策日志链路）；M-6 E2E 自身空转假 PASS 的教训与修法；"
                            "M-7 方案D：镜像对浏览器收不到的表只增不删（真实库实证不掉一行）。"
                            "**共同根因是 .catch(()=>{}) 吞掉了全部失败**；pytest 78 全绿，"
                            "空库（CI 场景）两个 E2E 脚本 PASS。"],
        ["剩余待办", "决策已全部落定并实现：Q-1/Q-2 推送与不拆分、Q-3 修去重键+覆盖计数、Q-4 常量不得驱动建议、"
                   "Q-5 K 三态、Q-6 维持严格 24h 口径、Q-7 决定不做（场景已失效）、Q-8 暂缓（写明触发条件）。"
                   "**代码侧已无待办**；只剩两类外部依赖："
                   "① 现场提供数据 T-12(501 灰分入库)、T-13(8-30 真实化验值)；"
                   "② 暂缓项 D-1~D-4（已列明再启动条件）。"],
        ["glm-5.3 二轮意见采纳情况", "采纳:计数修正(三1)、Q-6 标注为已决策(二7)、推送提为批次0(三2)、"
                              "拆分前先核实(三3)、AGENTS.md 写入多 agent 规则(四1)、"
                              "新增两条数据侧行动项(四2/四3,见 T-12/T-13)、"
                              "执行顺序(六1)。"
                              "其中三3 的核实结论:HEAD 之上已有 d87d06e,拆分需重写 2 个提交 → "
                              "按 glm 自己的判据「拿不准就保留混合提交」决定不拆。"],
        ["本轮新增发现", "后端 parse_coarse_factors 的 errors 是字符串列表,前端 parseCoarseFactors 的 errors 是 "
                    "{row,col,msg} 对象 —— 同一接口两种结构（见 T-14,低优先级）。"
                    "已在新增的导入 golden 里按「只比条数」规避,待 T-14 统一后再改为逐条对拍。"],
    ],
)

# ------------------------------------------------------------------ 2 待办清单
sheet(
    wb, "待办清单(含原因)",
    "可直接执行的待办清单（每项附原因）— T-1~T-10、T-14 已于 2026-09-10 完成，见「本轮已完成」表；本表保留原因与做法作为决策记录",
    ["编号", "优先级", "类型", "模块", "要做的事", "原因（为什么必须做）", "证据 / 位置", "具体做法", "预估", "前置依赖"],
    [8, 8, 12, 14, 34, 56, 40, 46, 10, 14],
    [
        ["T-1", "高", "缺陷修复", "导入解析",
         "浮精灰分/煤量单元格解析失败时返回 null，不再用 || 0 兜底",
         "空白或写「-」「？」的灰分单元格会变成 0%，并且作为一条真实记录 push 进 floatCoal；"
         "resolve_float_ash 取的是最新一条记录的灰分，于是 0% 会直接进入总精煤灰分公式，"
         "无声拉低总灰分并影响密度建议——现场从界面上完全看不出来。"
         "同一函数里紧邻的密度列做了 1.3~1.6 范围校验，灰分列却没有，属明显遗漏。",
         "frontend/js/import.js:716-717（float_ash 分支）\n同文件 :684-687（液位/水分/煤量同模式）\n后端对照 backend/app/services/resolvers.py resolve_float_ash",
         "解析失败返回 null（不参与该条记录的灰分字段），由 resolvers 的 _is_num 在取值链里丢弃；"
         "同时给该行记一条 skipped 以便导出错误记录时可见",
         "20 分钟", "无"],
        ["T-2", "高", "缺陷修复", "xlsx 读取",
         "xlsx-lite 补 inlineStr（<is><t>）分支，并把字符引用正则扩展到十六进制",
         "① t=\"inlineStr\" 的单元格（部分导出器与 WPS 会这样写）在浏览器读到 null，openpyxl 却能正常读到——"
         "同一文件在工作面列上「浏览器整列为空、后端有值」，属双轨分叉；"
         "② 本次只修了十进制 &#10;，十六进制 &#x0A; 仍留在文本里，导致「修了一半」。"
         "这两个缺口都在本次刚修过的同一段代码旁边，顺手补掉成本最低。",
         "frontend/js/xlsx-lite.js:215（只读 <v>，无 <is> 分支）\n同文件 :275（_decodeXml 只匹配 &#(\\d+);）\n"
         "对照 openpyxl worksheet/_reader.py 能读 inlineStr 且解码十六进制引用",
         "在单元格解析里补 <is><t>…</t></is> 分支并复用 _decodeXml；"
         "把正则改为 /&#(x?[0-9A-Fa-f]+);/g；"
         "同时删掉 import.js:439 与 importer.py 里多余的 &#10; replace，让「读取器负责解码」这一契约单一化",
         "40 分钟", "无"],
        ["T-3", "高", "文档", "工程规范",
         "更新 frontend/AGENTS.md：路径与工作目录规则",
         "该文件仍写着「本目录（D:\\Users\\liu\\Desktop\\测试\\web (2)）是唯一允许修改代码的地方」，"
         "而项目已整合到 D:\\dense-medium-density-control-system（backend/frontend/docs 同仓库）。"
         "任何读这份文件的 agent 都可能把改动写回桌面旧副本，或在错误的目录里找文件——"
         "这正是本次审查期间真实发生过的困惑来源。",
         "frontend/AGENTS.md 全文",
         "改为描述当前布局：三目录同仓库；后端改动落 backend/、前端落 frontend/、文档与 Excel 产出到 docs/；"
         "保留仍然有效的硬性规则「改 JS 必须同步递增 index.html 里的 ?v=N」",
         "15 分钟", "无"],
        ["T-4", "中", "测试基建", "导入解析",
         "新增 backend/scripts/dump_import_js.js，建立 parseCoarseFactors 的跨语言 golden",
         "brief / training / resolvers / seed 都有 dump_*_js.js 跨语言 golden，唯独导入解析没有——"
         "偏偏本次所有对拍风险都集中在这一块。本次修复只能靠我临时写的「从源码切片求值」脚本验证，"
         "这种脚本用完即弃、无法进 CI。补上之后，日期与列的任意一侧改动都会被立刻发现。",
         "对照 backend/scripts/dump_brief_js.js 的既有写法\n本次临时脚本已证明切片对拍可行（29 例日期 + 11 例序列）",
         "照 dump_brief_js.js 写 dump_import_js.js：向页面注入一组固定行，导出 parseCoarseFactors 结果；"
         "在 tests/test_importer.py 增加 golden 对拍用例",
         "50 分钟", "无"],
        ["T-5", "中", "健壮性", "导入解析",
         "年份常量化 + 跨年处理（JS 提同名 DEFAULT_YEAR，解析结果早于上一行时年份 +1）",
         "Python 用 DEFAULT_YEAR=2026，JS 在 3 处写字面量 2026——只改 Python 常量会静默破坏双轨对拍；"
         "日期辅助函数只返回 {m,d}，Excel 单元格里的真实年份被丢弃（2027-01-05 会变成 2026-01-05）；"
         "12.30 之后接 1.1 时候选月份不成立，会退回约一年前的时间戳。"
         "当前数据都在同一年内，所以是潜在缺陷而非现行故障——但一旦跨年就会一次性污染全部时间戳。",
         "backend/app/services/importer.py DEFAULT_YEAR 与 parse_ts\n"
         "frontend/js/import.js:396、:609、:613（字面量 2026）",
         "JS 侧提为与 Python 同名的 DEFAULT_YEAR 常量（三处共用）；"
         "日期辅助函数改为返回 {y,m,d}；解析结果早于上一行完整日期时把年份 +1；补 12月→1月 用例",
         "1 小时", "T-4"],
        ["T-6", "中", "健壮性", "同步守卫",
         "防回退守卫从「只比 coal_records」扩展为多业务表向量比较",
         "守卫目前只比 coal_records 总数：旧浏览器若该表数量相等或更多、但 calc_logs / heavy_samples 更少，"
         "仍会整体洗掉服务器侧的新补录与日志——守卫是「部分有效」，而它要防的正是这类结构性事故。"
         "先做多表向量比较成本低、不动架构；revision 方案留作后续彻底解。",
         "backend/app/services/migrate.py replace()（incoming/current 均只数 CoalRecord）",
         "统计各业务表数量做向量比较，任一表回退即拒绝并返回具体是哪张表；"
         "保留 force=true 语义不变；补一条「calc_logs 更少也应被拒」的用例",
         "40 分钟", "无"],
        ["T-7", "中", "数据治理", "口径一致性",
         "存量 influence_value 一次性重算迁移（__fixes.influence 标记）",
         "库里已有的浮精/粗精记录，其 influence_value 是用写死的重介灰分 8.50 算出来的，"
         "与现在的口径（502 在线 / 默认 7.9）不一致。显示层已经改为实时重算、不再读它，"
         "但这个字段仍会随整库镜像写回服务器并对外提供，留着旧口径值会持续误导下游使用它的人。",
         "frontend/js/import.js 与 collect.js 的历史写入值\n本次已将写入侧改为 App.getHeavyAsh()",
         "照 __fixes.julStray 的写法加一个一次性迁移：重算所有 floatCoal/coarseCoal 的 influence_value 并写回，"
         "记录条数到 importLogs",
         "40 分钟", "T-1（避免把 0% 灰分的记录一起重算成有意义的值）"],
        ["T-8", "中", "可用性", "K 标定",
         "决策日志导出（Excel/CSV）",
         "决策日志的设计目的就是给 K(工况) 标定提供「决策+响应」数据对，但现在只能通过 GET /api/v1/state "
         "看 JSON——现场人员拿不到、用不了，等于数据采了却无法进入标定流程。"
         "导出成表格是让这套观测闭环真正可用的最后一步。",
         "frontend/js/app.js densityDecisionLog（上限 200 条）\n"
         "查看方式见 app.js 顶部注释「GET /api/v1/state → densityDecisionLog」",
         "在合适的页面加入口，导出为表格：每行一条决策（时间/触发/方案/目标/ρ旧/ρ新/Δρ/ΔA/K 及来源/工况上下文/"
         "响应来源/滞后分钟/是否 invalidated），并用现有 XLSX.writeFile 落盘",
         "40 分钟", "无"],
        ["T-9", "低", "健壮性", "xlsx 读取",
         "xlsx-lite 支持自定义日期数字格式（与 openpyxl 同规则判定）",
         "浏览器只认内置 numFmtId（14-22/27-36/45-47/50-58），openpyxl 按格式字符串是否含 [dmhys] 判定。"
         "同一单元格 6.16 若套了自定义格式，JS 得到 6月16日、Python 得到 1月6日。"
         "当前两轨不会同时处理同一文件（浏览器自解析），所以不是现行故障；"
         "但它是「解析搬后端」这个既定计划的前置对拍缺口，不补就不能迁移。",
         "frontend/js/xlsx-lite.js:111-119\n对照 openpyxl styles/stylesheet.py is_date_format",
         "在 _readDateStyles 里解析 <numFmts> 段，用与 openpyxl 相同的 [dmhys] 规则判定；"
         "补一条自定义格式日期列的对拍用例",
         "1 小时", "T-4"],
        ["T-10", "低", "健壮性", "双轨同步",
         "统一「新旧」判据：让镜像守卫与自动拉取用同一个口径",
         "守卫按 coal_records 计数，autoPullIfStale 按 store 三个数组（含独立的 calc_logs 表）计数——"
         "两个判据不同，就可能出现「自动拉取判定服务器更新并覆盖本地」而「镜像被守卫拒绝」的组合，"
         "用户看到「导入完成」但数据没落库。修法取决于是否引入 revision，故排在决策之后。",
         "backend/app/services/migrate.py replace()\nfrontend/js/app.js autoPullIfStale()",
         "若采纳 revision：服务端自增计数 + 前端携带，守卫与拉取都按 revision 比较（「新者胜」）；"
         "若不采纳：把两侧计数口径统一为同一组表，并补一条端到端用例",
         "1 小时", "Q-8 决策"],
        ["T-11", "低", "交付流程", "版本管理",
         "推送 11 个未推送提交到 GitHub（origin/main）",
         "本地领先 origin/main 共 11 个提交（含本次审查与修复），长期不推会失去远端备份，"
         "也不便于在多台机器之间接力。推送前建议先确认拆分与提交信息（见 Q-1、Q-2）。",
         "git log origin/main..HEAD → 11 个提交",
         "确认后执行 git push origin main",
         "5 分钟", "Q-1 / Q-2 决策"],
        ["T-12", "中", "数据治理", "现场数据",
         "现场在表3（灰分密度表）开始记录 501 皮带灰分",
         "Q-4 的根本解在数据侧而非算法侧：真实库 360 条灰分密度记录**全部是 belt=502、零条 501**，"
         "所以 ash_501 恒为默认常量 8.8 —— 任何兜底算法都只是在替一个不存在的测量值打补丁。"
         "只要现场按与密度采样同一张表开始记录 501，总灰分直读就能真正生效，Q-4 的兜底问题自动消失。"
         "（来源：glm-5.3 二轮意见 四2）",
         "sqlite 只读查询：coal_records where category='ash_density' group by belt → 502:360 / 501:0\n"
         "backend/app/services/resolvers.py INSTRUMENT_DEFAULT['ash_501']",
         "与现场确认 501 灰分仪的读数是否可导出/可抄录；确认后在表3 增加 501 行（或独立列），"
         "前端与后端取值链已支持 belt='501'（brief 第 6 列、resolve_total_ash 均已就绪），无需改代码",
         "现场配合（代码 0）", "无"],
        ["T-13", "中", "数据治理", "现场数据",
         "补齐 8-30 表1（粗精煤泥）缺失数据：现场提供真实化验值后补录或重导",
         "实测确认真实库 8-30 当日 coarse 记录为 0 条（8-28/8-29/8-31 各 3~4 条），"
         "直接后果是简报缺 8-30 22:00 与 23:00 两行（应有 265 小时、实有 263）："
         "表1 最后一条是 8-29 22:00，到 8-30 22:00 结束已超 24h 续传窗口 → 该两小时不成行。"
         "glm-5.3 的诊断是「该日三行灰分列填的是时间导致被过滤」，症状（整日缺行）已核实一致；"
         "但原始行未入库，故具体原因需现场核对源表。"
         "（来源：glm-5.3 二轮意见 四3 + 本次实测）",
         "sqlite 只读:category='coarse' and ts like '2026-08-30%' → 0 条\n"
         "brief 缺行定位:2026-08-30 22:00 / 23:00",
         "请现场核对 8-30 的原始记录表；拿到真实 315 灰分后按现有导入流程补录（同时间戳会自动覆盖），"
         "生效后简报恢复连续 265 行；属数据治理，不涉及代码改动",
         "现场配合 + 5 分钟导入", "无"],
        ["T-14", "低", "健壮性", "接口一致性",
         "统一 parse_coarse_factors 的 errors 结构（后端字符串 vs 前端对象）",
         "同一份解析结果，后端 errors 是字符串列表（如 \"未识别到多因素表头\"），"
         "前端是 {row, col, msg} 对象 —— 前端能把错误行号直接呈现给用户，后端不能。"
         "本轮新增的导入 golden 因此只能按「错误条数」对拍，无法逐条比对内容，"
         "削弱了 golden 的检出能力；将来若把解析搬到后端（D-1），错误提示会直接退化。",
         "backend/app/services/importer.py errors.append(\"未识别到多因素表头\")\n"
         "frontend/js/import.js errors.push({ row, col, msg })",
         "把后端 errors 改为 {row, col, msg} 结构（row 从 1 计、与前端一致），"
         "然后把 golden 用例从「比条数」升级为「逐条对拍」；同时更新 import_api 的错误返回",
         "30 分钟", "无"],
    ],
    fill_col=2, fill_map={"高": HIGH_FILL, "中": MID_FILL, "低": LOW_FILL},
)

# ------------------------------------------------------------------ 3b 本轮已完成
sheet(
    wb, "本轮已完成",
    "本轮已完成事项与验证证据（2026-09-10 批次 0-2 · 2026-09-11 同步链路 M-1~M-7）",
    ["编号", "事项", "结果", "验证证据"],
    [8, 34, 66, 66],
    [
        ["批次0", "核实 git 状态 → 决策 Q-1/Q-2 → 推送",
         "12 个提交推送成功（84c6924..d87d06e），未推送数归零；远端与本地一致。"
         "Q-2 决定不拆分：核实发现 HEAD 之上已压了 d87d06e，拆分需重写 2 个提交，"
         "按 glm-5.3 自己的判据「拿不准就保留混合提交（内容正确>历史整洁）」放弃。",
         "git push 输出 84c6924..d87d06e main -> main；git log origin/main..HEAD 为空；"
         "推送前检查：46 个文件、1.9MB、无 .db/.env/密钥/临时文件"],
        ["T-1", "浮精/粗精灰分解析失败不再当 0",
         "灰分单元格为空白/「-」「？」/纯文本/0 时整行跳过并记入错误记录，不再生成 0% 的"
         "「真实读数」进入总灰分公式。校验位置放在「同时间戳覆盖」之前 —— "
         "否则跳过该行时旧记录已被删除，会变成净丢数据。",
         "浏览器实测（file://，不碰服务器库）：浮精 6 行（-/？/空/abc/0/9.5）→ 只导入 1 条 9.5，"
         "failed=5，anyZeroAsh=false；粗精 2 行（空/12.5）→ 只导入 1 条，failed=1"],
        ["T-2", "xlsx-lite 补 inlineStr 与十六进制字符引用",
         "① t=\"inlineStr\" 单元格（<is><t>）不再读成 null；② &#x0A; 与 &#10; 都能解码为换行；"
         "③ 码点用 fromCodePoint（>0xFFFF 不再截断）；④ 越界码点原样保留；"
         "⑤ 「&amp;#10; 保留为字面量」的既有契约未被破坏。",
         "手工构造最小 xlsx（含 inlineStr + 十六进制引用）端到端读取："
         "A1=\"3309\\n43下01\" 且字符码为 51,51,48,57,10,52,51,19979,48,49（含真实换行 10）；"
         "_decodeXml 单测：hex/dec/named/astral/越界/字面量 6 项全部符合预期"],
        ["T-3", "更新 frontend/AGENTS.md",
         "改为当前仓库布局（backend/frontend/docs 同仓库），删除「桌面 web (2) 是唯一允许改代码的地方」"
         "这条已失效的硬规则；补入双轨逐值一致的要求；按 glm-5.3 建议新增硬规则"
         "「同一时间只允许一个 agent 持有未提交改动」。",
         "文件已更新并被运行时重新加载；内容含 4 条硬性规则与页面/算法约定"],
        ["T-4", "新增导入解析跨语言 golden",
         "新增 backend/scripts/dump_import_js.js（Dump ImportPage.parseCoarseFactors），"
         "fixture 覆盖：文本两位小数=显式日、单小数位省尾零、序列消歧+跨月回退、"
         "Math.round 取整、畸形日期回退、多行工作面、坏灰分行跳过；"
         "新增 2 条 pytest 用例（fixture 语义断言 + 与 JS golden 全量逐字段 diff）。",
         "node scripts/dump_import_js.js → 9 条记录；前后端逐字段比较差异数 = 0；"
         "pytest 65 → 67 全绿；golden 文件缺失时用例自动跳过（无浏览器环境仍可跑 CI）"],
        ["T-5", "年份常量化 + 跨年处理",
         "年份不再写死：单元格自带年份（ISO 写法 / Excel 真实日期序列）优先，否则沿用上一行年份、"
         "再退回 DEFAULT_YEAR；**仅当上一行是 12 月且本行月份小于 12** 时年份 +1。"
         "刻意不用「月份变小就跨年」的宽规则——第一版实现用了它，结果手写表里月份写乱"
         "（7月里混一行 6.02）会把整批行抬到下一年，比时间戳略早更严重，已收窄。"
         "JS 侧新增与后端同名的 DEFAULT_YEAR 常量，替换 3 处字面量 2026。",
         "golden 新增年份 fixture：12.30 → 2027-01-01 → 2027-01-02 → 2027-03-05（ISO 自带年份优先），"
         "前后端逐字段一致；pytest 67 → 69"],
        ["T-6", "防回退守卫改为按类别细分的向量比较",
         "旧守卫只比 coal_records 总数：总数相等也可能已回退（ash_density 少 20 条、coarse 多 20 条 → 放行）。"
         "现按 coarse/float/ash_density + calc_logs + heavy_samples 逐项比较，任一项回退即拒绝并点名是哪一类。"
         "**刻意不含 manual_entries / import_logs / alerts**：前端「删除单条补录」「清空补录历史」会合法地"
         "让它们变少，纳入守卫会把正常操作误判成回退。",
         "新增 2 条用例：① 总数不变但 ash_density 回退 → 被拒且错误信息点名 ash_density；"
         "② manualEntries 清空 → 放行。pytest 69 → 71"],
        ["T-7", "存量 influence_value 一次性重算",
         "影响值口径统一到 App.influenceOfFloat / influenceOfCoarse（浮精页图表/表格/详情与存量重算同一份公式）；"
         "一次性迁移按 __fixes.influence 标记只跑一次。",
         "浏览器实测：2 条脏值被重算为 -0.012 / 4.222（与公式一致）；标记置位后再次调用完全不碰记录"
         "（人为写入 -99 保持不变）；已纳入 _applyServerState 的 localOnly 以免被服务器数据清掉而重跑"],
        ["T-8", "决策日志导出 Excel",
         "决策日志原本只能通过 GET /api/v1/state 看 JSON，现场拿不到、进不了标定流程。"
         "新增 App.exportDensityDecisionLog()（31 列：决策 + 工况上下文 + 响应三态）"
         "并在导入页「导入日志」面板加按钮；附「口径说明」Sheet，避免拿到表的人误读 invalidated 行。",
         "浏览器实测：2 条决策导出为「密度决策日志_2条.xlsx」，含 2 个 Sheet、范围 A1:AE3；"
         "空日志分支给出正确提示"],
        ["T-9", "xlsx-lite 支持自定义日期数字格式",
         "浏览器原来只认内置 numFmtId，openpyxl 按格式字符串（含 [dmhys]）判定 → 套了自定义日期格式的"
         "单元格在两侧会解析成不同日期（6.16 → 6月16日 vs 1月6日）。现按 openpyxl 同规则判定："
         "剥掉 [条件/颜色/区域] 段、引号内字面量、转义字符后再看是否含 dmhys。",
         "手工构造含 3 个自定义 numFmt 的 xlsx 实测："
         "`m.d` → 日期字符串；`0.00\"kg\"` → 仍为数字 12.5（引号字面量被正确剥离）；"
         "`[$-804]m/d/yyyy` → 日期字符串；无格式单元格 → 数字 99.5"],
        ["T-10", "统一「新旧」判据：镜像守卫与自动拉取同口径",
         "旧判据「三数组总数多者胜」与守卫（按表逐项比较）不同口径，可能组合出"
         "「自动拉取判定服务器更新并覆盖本地」而「镜像被守卫拒绝」的静默不一致。"
         "现按与守卫相同的三类测量记录逐项比较：服务器在每一类都不少且至少一类更多才拉取；"
         "两侧各有更全的部分则不自动覆盖并留 console 警告。",
         "实盘四情形实测：本地==服务器→不拉取；服务器多一类→拉取并提示逐类向量；本地多一类→不拉取；"
         "**混合情形专项证明**：本地 515 条 < 服务器 529 条（但本地 coarse 多 1 条）时，"
         "旧规则会拉取并抹掉本地多出的记录，新规则不拉取且 coarse 记录保留"],
        ["T-14", "统一导入解析 errors 结构",
         "后端 errors 是裸字符串、前端是 {row,col,msg} 对象 → 前端能给出错误行号、后端不能，"
         "新增的导入 golden 也只能比条数。现统一为 {row,col,msg}，错误文案两侧逐字一致。",
         "新增坏表头 fixture（既无「原煤灰分」也无「液位」）：两侧均 0 记录 + 1 条完全相同的错误对象；"
         "golden 用例从「比条数」升级为「逐条对拍 errors」"],
        ["Q-4", "501 常量：层级三态 + 常量不得驱动控制建议 + 简报列留空",
         "501 层级三态（manual/online/none）；界面「默认(仪表)」→「默认常量(501未接入)」；"
         "总灰分落到常量时 guidance 直接 valid=false 并给出原因（挡住「用编造灰分推出下调密度」）；"
         "简报 501 列在无该小时记录时留空、总灰分在公式不完整且无在线总灰分时留空；"
         "后端新增 resolve_total_ash_ex / ash501_layer，前端镜像同序同文案。",
         "新增 5 条 pytest（三态、来源、常量挡建议、真实读数不挡、简报列留空）；"
         "浏览器实测：公式不可用→layer=none/source=ash501/isConstant=true→guidanceValid=false；"
         "**前后端 reason 逐字一致**（自动比对 True）；简报 golden 重生成后 col6 全空、逐值对拍通过"],
        ["Q-3", "重复时间戳：修去重键 + 覆盖单独计数",
         "去重键原用**原始单元格文本** → 8:00 与 08:00:00 判不出重复而入库后同一条，检测形同虚设；"
         "改为用 normalizeTs 后的 ts + 系统。同时把「覆盖已有记录」单独计数（replaced）写进导入日志"
         "与完成提示——重新导入修正表按时间戳覆盖是必需功能，但不能静默。",
         "浏览器实测：4 行中 8:00/08:00:00(同系统) 判为重复、B 系统同时间不误判；"
         "覆盖场景 replaced=1、提示「覆盖已有记录1条」"],
        ["Q-5", "K 三态收敛成一句结论 + 可展开明细",
         "kInfo 带 state(data/unidentifiable/insufficient) 与 systems 明细；结论行给操作员看，"
         "分系统 k/n/R² 与「是否被拒」放悬停明细里。",
         "浏览器实测：kSource=default、state=unidentifiable，文案与分系统明细均正确渲染"],
         ["M-1", "缺陷1：守卫把 heavy_samples 纳入比较 → 浏览器→服务器同步全死",
          "前端把 heavySamples 视为纯本地键（不参与反向同步），新浏览器恒为 0、服务器有 1 → "
          "守卫每次都判「回退」并拒绝整库写。**自 ca08e85 起浏览器→服务器同步实际已失效**，"
          "只因失败被 .catch(()=>{}) 吞掉而无人察觉。修法：守卫只比 "
          "{coarse,float,ash_density,calc_logs}（与前端 _measureVector 同口径），"
          "并在 migrate.py 写明「不要再把 heavy_samples 加回来」。",
          "新增回归用例 test_stale_guard_allows_mirror_with_empty_local_only_collections；"
          "E2E 用例0（冷启动空配置 → 整库写）由 stale 拒绝转为 {'ok':true}"],
         ["M-2", "缺陷2：keepalive 请求体超 64 KiB 上限，关页面时的镜像全部失败",
          "页面隐藏/卸载时用 fetch(..., {keepalive:true}) 发 171 KB 整库快照，超过 Chrome/Edge 的 "
          "64 KiB 硬上限 → TypeError: Failed to fetch，同样被静默吞掉。修法：只在装得下时才用 "
          "keepalive（MIRROR_KEEPALIVE_MAX=60000），装不下退回普通 fetch，并统计两类次数。",
          "新增 _mirrorStats.keepalive/noKeepalive 计数；实测整库 175 KB → 普通 fetch 可送达，"
          "带 keepalive 必失败（对照复现）"],
         ["M-3", "缺陷3：修完守卫后，镜像开始把服务器上的 heavy_samples 洗成 0",
          "M-1 只收窄了**比较**、没收窄**写入**：守卫放行后镜像把整库（含空 heavySamples）覆盖上去，"
          "服务器 1 条化验样被清零。教训：改了准入条件就必须同时检查写入的**所有权**。"
          "修法：镜像不拥有 heavy_samples（force=False 时保留原表），force=true 才整库重写。",
          "新增 test_mirror_does_not_wipe_heavy_samples；E2E 实测真实浏览器镜像后服务器样本条数不变；"
          "并清理了验证过程产生的合成样本（source='verify'）"],
         ["M-4", "② 写路径禁止静默吞错：结果返回 + 待发副本 + 两类失败分开处置",
          "putStateBody 末尾的 .catch(()=>{}) 是三个缺陷长期隐藏的共同原因。现改为返回 "
          "{ok:true} / {ok:false,rejected,reason} / {ok:false,error}，并分两类：守卫明确拒绝"
          "（rejected）→ 不留待发副本（重试结果必然相同，数据对齐交给 autoPullIfStale），只记状态"
          "并提示一次；传输失败（后端未启动/keepalive 超限/断网）→ 整库快照存入 "
          "localStorage['dmcs_mirror_pending']（单槽，因为每次都是全量快照），在 online / 切回前台 / "
          "下次启动 / 下次保存时重试，成功才清除。",
          "新增 E2E 用例1（注入发送失败 → 断言留下待发副本+提示+状态；恢复后 online 触发重试 → "
          "断言服务器真实收到新值，且读的是 /api/v1/state 而非 stub）；pytest 78 全绿"],
         ["M-5", "① 端到端脚本进 CI：用例0 + 完整输出 + 任一 FAIL 即非 0 退出",
          "缺陷1 本可以被一条 E2E 用例当场抓住，但脚本只在个人机器上手跑 → 没有执行就没有防线。"
          "新增 backend/scripts/verify_mirror_e2e.js（用例0 冷启动整库写不得被守卫拒绝；"
          "用例1 失败不静默 + 重试送达），与 verify_decision_log.js 一起接入 "
          ".github/workflows/ci.yml 的新 e2e 作业（windows-latest 自带 Edge；固定 Node 22 "
          "因为脚本用全局 WebSocket；自己起后端并轮询就绪；不做任何输出截断）。",
          "本机以**空库**（= CI 场景，全新 SQLite）复跑两个脚本：镜像 7 项断言全 PASS、决策日志 "
          "PASS，退出码均为 0；空库下 GET /state 只有 14 键且 ashTarget 为 null，基线逻辑已适配"],
         ["M-7", "方案D：整库镜像对「浏览器收不到的表」改为只增不删",
          "原先整库 PUT 会 wipe 并按 payload 重写 manual_entries / import_logs / alerts / "
          "regression_models / coarse_model_history，而 GET /state **不返回**它们、后端也只由镜像写入"
          "—— 两端不可能收敛：纳入守卫会让「列表较短」的浏览器永久拒写（M-1 病理），"
          "照删照写又会在多浏览器/清库场景下静默删掉服务器上的行。"
          "改为按自然键合并、只增不删（force=true 仍整体覆盖）。",
          "新增 3 条 pytest（短列表镜像不删历史、新行能上行且不重复插、告警条件消失不清库），"
          "pytest 78 → 80 全绿；**真实库实证**：发送 manualEntries/alerts/importLogs 全空的镜像后，"
          "manual_entries 242 / import_logs 3 / alerts 1 / coarse_models 2 / coarse_model_history 1 "
          "全部不变，响应带 merged 计数；E2E 与决策日志脚本在新后端上复跑仍全 PASS。"
          "后续项：「清空补录历史」的界面文案应说明只清本地，或加一个显式的 force 清服务器入口。"],
         ["M-6", "E2E 自身的空转假 PASS（方法论教训，已修）",
          "第一版用例1 把探针值设成 8.61，而服务器遗留值恰好也是 8.61 → 新快照与「上次已发送内容」"
          "逐字节相同，被「内容相同则跳过」短路，**什么都没发**；后续断言又恰好对上遗留值，"
          "于是 4 条断言全绿却是空转。现在：探针值必须同时不同于服务器值与浏览器值，"
          "并额外断言 stub 被调用次数 === 1（防空转），失败时打印 debug 快照。",
          "诊断脚本逐步打印 _mirrorPending/_mirrorSent/待发键/stats，确认机制本身正常"
          "（calls:1 / failures:1 / saved:yes / 提示出现）后才定位到是用例自己的问题"],
    ],
    fill_col=1,
    fill_map={"批次0": OK_FILL, "T-1": HIGH_FILL, "T-2": HIGH_FILL, "T-3": HIGH_FILL, "T-4": MID_FILL,
              "T-5": MID_FILL, "T-6": MID_FILL, "T-7": MID_FILL, "T-8": MID_FILL,
              "T-9": LOW_FILL, "T-10": LOW_FILL, "T-14": LOW_FILL,
              "M-1": HIGH_FILL, "M-2": HIGH_FILL, "M-3": HIGH_FILL,
              "M-4": MID_FILL, "M-5": MID_FILL, "M-6": LOW_FILL, "M-7": HIGH_FILL},
)

# ---------------------------------------------------------------- 3c 待现场提供
sheet(
    wb, "待现场提供(数据)",
    "需要现场提供或确认的数据（代码已就绪，只等数据）",
    ["编号", "要收集什么", "为什么需要它", "拿到之后会怎样", "谁来做"],
    [8, 34, 62, 56, 20],
    [
        ["T-12", "表3（灰分密度表）开始记录 501 皮带灰分；"
                 "并确认 501 灰分仪的读数能否导出或抄录",
         "真实库 360 条灰分密度记录**全部是 belt=502、零条 501**。"
         "「501 = 总混配皮带，其读数即在线总灰分」这个口径本身是对的，但没有数据源 → "
         "ash_501 恒为默认常量 8.8，简报第 6 列恒显 8.80。"
         "任何兜底算法都只是在替一个不存在的测量值打补丁，根本解在数据侧。",
         "取值链已支持 belt='501'（brief 第 6 列、resolve_total_ash、前端 resolveInstrument 均已就绪），"
         "**无需改代码**：数据一到位，总灰分直读自动生效，Q-4 的兜底问题随之消失。",
         "现场（需与我确认采集方式）"],
        ["T-13", "8-30 当天粗精煤泥（表1）的真实化验值；"
                 "并核对源表为何整日无有效行",
         "实测确认 8-30 当日 coarse 记录为 0 条（8-28/29/31 各 3~4 条），"
         "直接后果是简报缺 8-30 22:00 与 23:00 两行（应有 265 小时、实有 263）："
         "表1 最后一条是 8-29 22:00，到 8-30 22:00 结束已超 24h 续传窗口。"
         "glm-5.3 的判断是该日三行灰分列填的是时间导致被过滤，症状已核实一致，"
         "但坏行根本没入库，具体原因需现场核对源表。",
         "拿到真实值后按现有导入流程补录（同时间戳会自动覆盖），简报恢复连续 265 行；"
         "若确认是「灰分列写成时间」，现有解析已会跳过该行并记入导入错误记录，可直接导出核对。",
         "现场"],
        ["Q-4", "口径确认：501 无在线值时，总灰分兜底取什么？",
         "与 T-12 相关但属口径选择：① 用 502 推算 ② 保持常量 8.8 但界面标注「默认常量」而非「仪表」。"
         "在 501 数据到位前，这条决定界面显示什么。",
         "决定后约 30 分钟可完成（涉及 resolve_total_ash 兜底与层级文案）。",
         "用户/现场"],
    ],
    fill_col=1,
    fill_map={"T-12": MID_FILL, "T-13": MID_FILL, "Q-4": ASK_FILL},
)

# ------------------------------------------------------------------ 3 待决策
sheet(
    wb, "待你决策的问题",
    "需要你（或现场）确认后才能动手的问题",
    ["编号", "优先级", "问题", "背景与原因", "选项", "我的建议"],
    [8, 8, 34, 60, 52, 46],
    [
        ["Q-1", "已决", "11 个未推送提交是否现在推送到 GitHub？",
         "本地领先 origin/main 共 11 个提交（fe719e8 … dff835b）。推送后即对外可见，"
         "若还想调整提交结构（见 Q-2），应先在本地做完再推。",
         "① 现在就推 / ② 先调整提交结构再推 / ③ 暂不推送",
         "【已决 2026-09-10】已推送：84c6924..d87d06e（12 个提交），未推送数归零。"
         "依 glm-5.3 二轮意见五1「11 个提交只在本地是最大单点风险」。"],
        ["Q-2", "已决", "是否把 dff835b 拆成两个提交（docs 一个、代码修复一个）？",
         "该提交原本是另一个 agent 的「docs: 阶跃实验文档写回原文件名」，"
         "但它把我这次 9 个文件的代码改动一并提交了，我暂时只修正了提交信息。"
         "内容是对的、未推送，所以拆分成本低；不拆则历史里代码与文档混在一个提交。",
         "① 拆成两个提交（docs / review+fix） / ② 保持现在的一个提交",
         "【已决 2026-09-10：不拆】按 glm-5.3 三3 的要求核实后：dff835b 之上已压了 d87d06e，"
         "拆分需重写 2 个提交；依其五1 判据「拿不准就保留混合提交（内容正确>历史整洁）」放弃拆分。"],
        ["Q-3", "已决", "同一文件内出现重复时间戳时怎么处理？",
         "现在按时间戳逐条 filter 再 push，第二条会挤掉第一条（后者胜），但 imported 计数两条都算；"
         "最典型的触发是时间列空白——两轨都会落到 00:00:00，同一天多行最后只剩一行。"
         "数据库侧 UniqueConstraint(category,ts,system) 也无法表示重复，且 system 恒为「合并」。",
         "① 给重复时间戳排秒（+i 秒）全部保留 / ② 显式拒绝并计 skipped、导出错误记录 / ③ 维持后者胜但把计数改对",
         "【已决 2026-09-10：修前置 + 覆盖单独计数，不做一刀切拒绝】"
         "先修去重键（原实现用原始单元格文本做键，8:00 与 08:00:00 判不出重复 → 检测形同虚设），"
         "再把「覆盖已有记录」单独计数并显示在提示与日志里（重新导入修正表按时间戳覆盖是必需功能，"
         "拒绝重复会破坏它）。实测：8:00/08:00:00/08:00 三种写法中同系统同时间的被判重复、"
         "不同系统不误判；覆盖计数 replaced 与提示均已生效。"],
        ["Q-4", "已决", "501 皮带灰分仪无在线值时，总灰分兜底取什么？",
         "「501 = 总混配皮带，其灰分仪读数即在线总灰分」这个口径是对的，但实测真实库 360 条灰分密度记录"
         "全部 belt=502、零条 501 —— 也就是说 ash_501 目前恒为默认常量 8.8，简报第 6 列恒显 8.80。"
         "改动前的 501/502 加权虽然口径不对，但至少用了真实的 502 读数。",
         "① 501 无值时改用 502 推算的总灰分 / ② 保持常量 8.8 但在界面上明确标注「默认常量」而非「仪表」 / ③ 其他口径",
         "【已决 2026-09-10：采纳②并加一条硬约束，**不采纳①**】"
         "①「用 502 推算」正是改造前被判定为口径错误的加权平均（502 只是重介组分，重复计入），"
         "不重新引入。落地三件事："
         "(a) 501 层级三态 manual/online/none，界面文案由「默认(仪表)」改为「默认常量(501未接入)」；"
         "(b) **常量灰分不得驱动控制建议**——兜底落到常量时 guidance 直接 valid=false 并说明原因"
         "（否则 8.8−8.5=0.3 超容差会推出「下调密度」，等于用编造的灰分指挥现场操作）；"
         "(c) 简报 501 列在无该小时 501 记录时留空、总灰分在公式不完整且无在线总灰分时留空，"
         "不再让常量出现在导出表里冒充实测。前后端同序同文案（已逐字对拍一致）。"],
        ["Q-5", "已决", "是否在界面上展示 K 增益的三态（data / 数据不可辨识 / 样本不足）？",
         "实测数据驱动 K 的两个系统斜率均为负（A −0.0221、B −0.0196，R²≈0.05），被正性约束全部丢弃，"
         "因此永远回退 0.075、valid 恒为 false。这本身印证了「闭环数据不可辨识 K」的结论，"
         "但界面上看不出来，容易被误认为「已经用数据标定过了」。",
         "① 展示三态并给出 systems[].k 与 R²（让现场看到斜率是负的） / ② 只显示「不可辨识」 / ③ 不展示",
         "【已决 2026-09-10：采纳①，但呈现收敛成「一句结论 + 可展开明细」】"
         "不把统计量直接甩给操作员：结论行=「数据不可辨识 → 回退经验值 0.075：斜率越出物理约束"
         "（密度↑→灰分↑）已被拒，属闭环数据固有性质，需按《密度阶跃实验》开环标定」；"
         "悬停「［分系统明细］」看每套系统的 k/n/R² 与是否被拒。"
         "实测：系统A k=−0.0122 n=62 R²=0.028、系统B k=−0.0231 n=48 R²=0.142，均被拒 → 三态判为"
         "「不可辨识」而非「样本不足」。这项本质上是给阶跃实验做说服工作。"],
        ["Q-6", "已决", "简报在缺表时段的期望行为？",
         "现行规则是「三表在该小时内 24h 内均有记录才成行」，实测真实库因此得到 263 行（旧规则 1069 行是错位续传凑的）。"
         "但浮精每天只化验 1 次，所以只有每次采样后的 24h 内成行；浮精表 5-26~8-22 无数据，该段简报为空。",
         "① 维持严格 24h 三表齐全 / ② 缺表也出行，缺失列标注「缺」并用最近值 / ③ 时间窗口可配置（如 48h）",
         "【已决 2026-09-10：维持①，不再重开】严格口径「三表齐全才成行(24h 窗口)」当日已上线，"
         "实测真实库 263 行（8-23~9-3 连续），旧规则 1069 行是错位续传凑出来的无意义行。"
         "除非现场反馈「当班趋势看不到」，否则不改；若将来要改，属需求变更，须先明确现场用途。"],
        ["Q-7", "不做", "在界面上删除记录后，如何与服务器同步？",
         "删除后整库镜像会被防回退守卫拒绝（记录数变少），而下次开页 autoPullIfStale 又会把服务器数据拉回来，"
         "表现为「删了又回来」；用户从界面上看不到任何提示。",
         "① 删除类操作走 force=true 强制同步 / ② 被拒时给明确提示并提供「强制同步」按钮 / ③ 前端不允许删除，只允许作废标记",
         "【已决 2026-09-10：不做，场景已失效】T-6 之后前端**唯一**能缩小「被守卫集合」的入口"
         "（collect.js 删除单条补录）删的是 manualEntries，而它已被排除在守卫之外；"
         "其余写入路径都是同时间戳覆盖（净中性）。即「删了又回来」在当前代码里已不存在，"
         "只剩诊断可见性这一点残余价值，不值得现在动。若将来出现别的缩小路径再补 toast + 强制同步按钮。"],
        ["Q-8", "暂缓", "是否启动「把 Excel 解析搬到后端」的迁移？",
         "/import/parse 端点已存在但前端无任何调用者（全仓库只有文档生成脚本把它列为计划）。"
         "迁移的收益是解析逻辑单一化、可用 Python 测试与 golden 覆盖；"
         "风险是必须先补齐对拍，否则历史数据会整体错位。",
         "① 现在启动（先做 T-9 + T-4 再迁） / ② 暂缓，等 PLC 接入告一段落 / ③ 不迁移",
         "【已决 2026-09-10：暂缓，并写明触发条件】T-9（自定义日期格式对拍）与 T-4（导入 golden）"
         "已落地，**迁移的前置护栏已经具备**，所以这不再是风险问题、纯粹是收益/时机问题。"
         "触发条件：出现第二个需要导 Excel 的人/机器，或浏览器解析出一次实际事故。"
         "另注：/import/parse 仍是半成品端点（无调用者 + 按 Sheet 合并的逻辑与浏览器不同源），"
         "其畸形日期崩溃已修；若半年内不迁移就删掉，别让它长期以「看似可用」的状态存在。"],
         ["Q-9", "待决", "整库镜像对 manual_entries / import_logs / alerts / coarse_models 的**所有权**"
                 "（2026-09-11 由独立评审 + 实测确认）",
          "migrate.replace 对 force=False 也会 wipe 并重写这些表，而防回退守卫**不保护**它们"
          "（刻意排除，因为前端「清空补录历史」会合法地让它们变少）。"
          "同时前端只用到 GET /state、PUT /state、POST /training/coarse-model —— "
          "**整库镜像就是浏览器侧补录/导入日志/告警上行的唯一通道**。"
          "于是形成风险：某个浏览器手里的列表比服务器短（另一台浏览器新增过、或本地被清过），"
          "它的一次镜像就会把服务器上的那些行**静默删掉**（守卫不拦）。",
          "方案A 维持现状（单浏览器使用假设）：成本 0，风险=多浏览器/清库场景下静默丢这些行；"
          "方案B 守卫扩展到这些表 + 显式清空走 force=true：需要前端把「我确实清空了」作为显式意图上报；"
          "方案C 前端改为逐条调用 POST /manual-entries 等接口，镜像不再拥有这些表：最干净，"
          "但要新增前端调用与错误处理（工作量中等）；"
          "方案D 服务器对这些表改为「按 ts 合并、只增不删」。",
          "【2026-09-11 修正：原先建议的方案B 经实现前核查**不成立**，已撤回】"
          "关键事实：GET /api/v1/state 只返回 24 个键，**不含** manualEntries / importLogs / "
          "alerts / regressionModels / heavySamples —— 浏览器永远拿不到服务器上的这些行；"
          "而后端除 /manual-entries 的 POST（前端无调用者）外没有任何地方写 Alert / ImportLog，"
          "这些表**只由浏览器整库镜像写入**。把这类表纳入守卫，等于让守卫去比较一个两端"
          "**不可能收敛**的集合：只要某台浏览器的列表比服务器短，它的每一次镜像都会被拒，"
          "而它永远无法通过 GET 补上缺失的行 → **永久拒写**，正是缺陷 M-1 的病理。"
          "alerts 还更特殊：checkAlerts() 每次刷新都重建该数组（派生值，合法变少），"
          "纳入守卫会在告警消失时立刻开始拒写。另注：force=true 会连 heavy_samples 一起整体重写，"
          "所以「显式清空走 force」在支持**按表 force** 之前也不能直接用。"
          "因此可行的是方案C（前端逐条上行，镜像不再拥有这些表）或方案D（服务器只增不删/按 ts 合并）。"
          "【已决 2026-09-11：采用方案D 并已实现】migrate.replace 对 force=False 不再 wipe 这些表，"
          "改为按自然键合并（manual_entries: ts+category；import_logs: ts+category+filename；"
          "alerts: ts+system+message；regression_models: model_type+created_at；"
          "coarse_model_history: trained_at+production），只插入库里没有的行、**不删除**任何已有行；"
          "force=true（备份恢复）仍是整体覆盖。真实库实证：发送 manualEntries/alerts/importLogs "
          "全为空的镜像后，服务器的 manual_entries 242 / alerts 1 / import_logs 3 **一条不少**。"
          "代价（已知并接受）：界面「清空补录历史」不再删除服务器侧历史行——"
          "若要在界面上真正清服务器，需要单独的 force 入口（见 M-7 的后续项）。"],
    ],
    fill_col=2, fill_map={"高": HIGH_FILL, "中": MID_FILL, "低": LOW_FILL},
)

# ------------------------------------------------------------------ 4 暂缓
sheet(
    wb, "暂缓项与原因",
    "建议暂缓或明确不做的事项（附原因）",
    ["编号", "事项", "为什么暂缓", "什么条件下再启动"],
    [8, 30, 76, 52],
    [
        ["D-1", "把 Excel 解析整体搬到后端 /import/parse",
         "端点当前无调用者，浏览器自解析工作正常；而迁移前必须先补齐对拍（T-9 自定义格式、T-4 导入 golden），"
         "否则同一批历史文件在两轨会得到不同日期（已实测：自定义格式下 6.16 会变成 1月6日）。"
         "现在迁移等于在没护栏的情况下换掉正在用的轮子。",
         "T-9 与 T-4 完成、且 PLC 接入主线告一段落后（对应 Q-8）。"],
        ["D-2", "决策日志改为独立接口增量追加",
         "目前 200 条上限、约 80KB，随整库镜像一起 PUT 的代价可以接受；"
         "真正需要优化是在日志开始大量积累之后，现在做属于过早优化。",
         "决策日志接近 200 条上限、或整库镜像明显变慢时。"],
        ["D-3", "后端 _to_num 接受 \"inf\"/\"1e400\" 会生成 inf 并可能 500",
         "受影响的只有 /import/parse，而该端点无调用者（同 D-1）；"
         "修它属于迁移准备工作的一部分，单独修没有收益。",
         "随 D-1 的后端解析迁移一起处理。"],
        ["D-4", "引入 revision 让「新者胜」（彻底替换「多者胜」）",
         "这是比 T-6 更彻底的解法，但需要同时改后端表结构、PUT /state 契约与前端判据，"
         "属架构级改动；而 T-6 的向量比较已能覆盖当前已知的洗库通道。先用低成本方案堵住已知缺口，"
         "等实测再出现「数量多但内容旧」的真实案例再上 revision。",
         "T-6 上线后仍出现误判，或需要多端并发写入时。"],
    ],
    fill_col=1,
)

# ---------------------------------------------------------------- 5 glm 二轮审查（原样保留）
# glm-5.3 在 2026-09-10 直接把意见写进了本 Excel（提交 d87d06e）。
# 本脚本重新生成文件会覆盖工作区，故把该 Sheet 转存为 backend/data/todo_review_round2.json
# 并在此原样重建，避免"重新生成一次就丢掉别人的评审内容"。
# 快照来源：git show d87d06e:docs/待办-后续优化计划.xlsx → Sheet「第二轮审查意见」。
ROUND2 = Path(__file__).resolve().parent.parent / "data" / "todo_review_round2.json"
if ROUND2.exists():
    data = json.loads(ROUND2.read_text(encoding="utf-8"))
    rows = data["rows"]
    headers, body = rows[0], rows[1:]
    name = data.get("sheet", "第二轮审查意见")
    sheet(
        wb, name,
        "第二轮审查意见（glm-5.3，2026-09-10；由本脚本从 d87d06e 的快照原样重建）",
        headers, [10, 12, 78, 62], body,
        fill_col=2,
        fill_map={"总体评价": OK_FILL, "事实核验": OK_FILL, "文档问题": MID_FILL,
                  "盲区补充": HIGH_FILL, "决策倾向 Q-1/Q-2": ASK_FILL, "决策倾向 Q-3": ASK_FILL,
                  "决策倾向 Q-5": ASK_FILL, "决策倾向 Q-7": ASK_FILL, "决策倾向 Q-8": ASK_FILL,
                  "执行顺序建议": MID_FILL},
    )

# ------------------------------------------------- 6 同步链路缺陷复盘（2026-09-11）
sheet(
    wb, "同步链路缺陷复盘",
    "浏览器→服务器镜像链路：三个缺陷的共同根因与可复用的判据（2026-09-11）",
    ["环节", "事实", "为什么会长期没被发现", "可复用的判据"],
    [22, 74, 60, 62],
    [
        ["共同根因", "三个缺陷（守卫误拒 / keepalive 超限 / 镜像洗样本）都发生在**同一条写路径**上，"
                 "而该路径末尾是 .catch(() => {})：任何失败都只表现为「服务器没收到」，前端界面完全正常。",
         "镜像被设计成「尽力而为的副本」，于是「失败」在当时看起来是允许的；"
         "但「允许失败」不等于「允许不报告失败」——前者是可用性取舍，后者是失去了观测能力。",
         "写路径必须区分「尽力而为」（可失败）与「静默」（不可接受）：失败至少要有状态、计数或提示之一。"],
        ["准入 vs 所有权", "M-1 只收窄了守卫的比较范围，没收窄镜像的写入范围 → 放行之后整库覆盖立刻洗掉了"
                     "服务器的 heavy_samples（M-3）。",
         "修复一条链路时注意力在「让请求通过」，而「通过之后会写什么」是另一个方向的问题；"
         "回归用例当时只断言「写入被接受」，没有断言「接受后各表的内容」。",
         "改了「准入条件」必须同时检查「写入所有权」；回归断言要覆盖**写入结果**，不能只覆盖**是否放行**。"],
        ["空转的测试", "M-6：E2E 的探针值恰好与遗留值相同 → 新快照与上次已发送内容逐字节相同 → "
                   "被「内容相同则跳过」短路，断言在遗留状态上全绿。",
         "断言只检查了「最终状态对不对」，没有检查「这一步到底有没有发生」；"
         "而「没发生」和「发生了且正确」在只看终态时无法区分。",
         "每个用例都要有一条「这一步确实执行了」的断言（调用计数/副作用），"
         "并且输入必须与初始状态**可证明地不同**。"],
        ["用 stub 验证被 stub 掉的层", "早期验证把 Api.putStateBody 打桩后断言调用次数，而真正坏掉的"
                                "正是这一层的实现，于是得到「22 → 2，看起来很好」的错误绿灯。",
         "打桩让测试更容易通过，恰好绕过了唯一出问题的地方。",
         "只允许用打桩**注入失败**，不允许用打桩**替代成功路径**；成功路径必须走真实网络并回读服务器状态。"],
        ["没有执行就没有防线", "M-1 的缺陷本可以被一条 E2E 用例当场抓住，但该脚本只在个人机器上手跑，"
                        "长期无人执行。",
         "手工脚本的「可用性」依赖个人自觉，而自觉不是机制。",
         "能进 CI 的必须进 CI；跑不动的（依赖生产库数据的对拍）要写明「何时必须手跑」，"
         "并把输出完整保留（截断会藏掉 FAIL 行）。"],
    ],
    fill_col=1,
    fill_map={"共同根因": HIGH_FILL, "准入 vs 所有权": HIGH_FILL, "空转的测试": MID_FILL,
              "用 stub 验证被 stub 掉的层": MID_FILL, "没有执行就没有防线": LOW_FILL},
)

# ------------------------------------- 7 写路径独立评审与修复（2026-09-11）
sheet(
    wb, "写路径评审与修复",
    "独立评审（只读、未运行脚本）对镜像写路径改动的发现与处置（2026-09-11）",
    ["编号", "严重度", "发现", "处置与证据"],
    [8, 10, 72, 78],
    [
        ["R-1", "高", "api.js 把**任何** ok:false 都判为 rejected，而后端 migrate.replace 的 except 分支"
                "（如 SQLite 写锁超时）同样返回 ok:false 但没有 stale → 服务器**瞬时**异常被当成"
                "「永久拒绝、不留副本不再重试」，正是这次要根除的静默丢失被固化成设计。",
         "已改为 `rejected: j.stale === true`，其余 ok:false 归入可重试的传输/瞬时失败。"
         "证据：migrate.py:319-321 的 except 分支只返回 {ok:false,error}。"],
        ["R-2", "高", "待发副本只在 promise 回调里写：页面关闭/切后台时那次请求被浏览器中断，"
                "回调永不执行 → **「关页面丢最后一笔」这个原始痛点其实没解决**；"
                "且槽里若残留更旧快照，下次启动会把它推回服务器（守卫只比条数，可能放行）。",
         "已改为**发送前**先落副本（语义：本地已保存、服务器未确认），成功或被守卫拒绝后再清。"
         "E2E 新增断言 slotRightAfterSend（发送返回前槽里就必须有内容）。"],
        ["R-3", "高", "keepalive 上限用 body.length（字符）去卡 64 KiB（字节）：整库快照以中文为主，"
                "JSON.stringify 不转义非 ASCII，1 字符 = 3 字节 → 以为装得下、实际 3 倍超限，"
                "缺陷B（keepalive 超限 → TypeError）原样残留。",
         "改用 Blob([body]).size 取**字节数**，阈值降到 48 KB。"],
        ["R-4", "高", "E2E 用例0 在 CI 必然**空转假 PASS**：守卫是 `if sum(current.values()) > 0 and regressed`，"
                "空库上直接关闭；而 CI 的库必然是空的（*.db 被 gitignore）。"
                "即：即使把 heavy_samples 加回守卫，用例0 也照样绿。",
         "已改为先播种（records + samples/heavy-ash）并断言 GUARD_ARMED；"
         "并做了**反向对照**：临时把 heavy_samples 加回守卫后重跑，"
         "MIRROR_COLD_START 立刻 FAIL（stale_store ... heavy_samples 0<1），"
         "撤销后恢复 PASS —— 证明用例0 真的有牙齿，而不是只会绿。"],
        ["R-5", "高", "E2E 脚本对着**真实开发库**跑是破坏性的：整库 PUT 会 wipe/重写 "
                "manual_entries/alerts/import_logs/coarse_models/settings/auto_state，"
                "而脚本只复原了 ashTarget。",
         "已加前置检查：目标库非空则**直接拒跑**（退出码 2）并说明原因，"
         "需显式 DMCS_ALLOW_REAL_DB=1 放行。另新增 MIRROR_KEEPS_HEAVY_SAMPLES 断言"
         "（没有 GET 采样接口，用「新插入的自增 id 是否在原 id 之后」判断表没被清空）。"],
        ["R-6", "中", "用例1 只覆盖传输失败，新的 rejected 分支零覆盖；"
                "且 MIRROR_RETRY_ON_ONLINE 只断言结果、不断言因果（任何一次成功都能满足）。",
         "新增用例2（注入 rejected：必须不留待发副本、记 rejected 计数、给 info 提示、"
         "内容相同的再次保存不得重发）；新增 MIRROR_NOT_DELIVERED_YET 断言"
         "（注入失败后服务器必须仍是基线值），让「重试送达」具备因果前提。"],
        ["R-7", "中", "跨标签页会丢数据：待发槽是裸 body、无归属无时间戳，成功时无条件清除 → "
                "B 标签页的成功会删掉 A 标签页尚未送达的唯一副本；旧标签页的失败还会覆盖新快照。",
         "槽内结构改为 {wall, stamp, body}：写入时若已有更新的快照则不覆盖（wall），"
         "清除时只清自己写的那份（stamp）。"],
        ["R-8", "中", "rejected 分支不清槽、不更新 _mirrorSent → 每次 online/切前台都会重发一次"
                "注定被拒的整库；提示语「正在自动反向同步」是假的（autoPullIfStale 只在启动跑一次）。",
         "rejected 现在会清掉自己那份副本、记入 _mirrorSent（内容不变不再重发），"
         "并在失败 streak 的第一次**真的**调用 autoPullIfStale()，让提示语成真。"],
        ["R-9", "中", "CI：e2e 作业无 timeout-minutes（默认挂 360 分钟）、"
                "就绪探测号称 60 秒实际更久、Edge 候选三处不一致、无收尾步骤（自托管会留僵尸占端口）。",
         "已加 timeout-minutes: 15、短超时探针 + 真实计时输出、Edge 候选统一为同一份列表、"
         "新增 if: always() 的停后端步骤。另把两个脚本的 CDP 端口改为随机，避免连到遗留浏览器。"],
        ["R-12", "中", "方案D 的代价：镜像不再删除这些表 → 界面「清空补录历史」只清本地，"
                 "服务器侧历史行会保留（用户从界面看不出来，因为 GET /state 本来就不返回这些表）。",
         "已在 Q-9 与 M-7 写明。后续项：给「清空补录历史」加文案说明，或加一个显式的"
         "「同时清除服务器历史（force）」入口——这需要按表 force（现在 force=true 会连 "
         "heavy_samples 一起整体重写）。"],
        ["R-11", "高", "（我自己上一版的建议错了，实现前核查时发现）曾建议「方案B：把 alerts / "
                 "import_logs 纳入防回退守卫」。但 GET /state 不返回这两张表，浏览器永远拿不到"
                 "服务器上的行，而它们只由整库镜像写入 —— 纳入守卫会让「列表较短」的浏览器"
                 "**永久拒写**（守卫比较两端不可能收敛的集合），即缺陷 M-1 的病理；"
                 "alerts 还是每次刷新重建的派生数组。",
         "已**撤回该建议**并写入 Q-9 的修正说明。教训：判断「某表能不能进守卫」的标准不是"
         "「它会不会被用户清空」，而是「两端能否收敛」——守卫只能比较双方都能拿到同一份数据的集合。"],
        ["R-10", "中", "去重判据位置不对：saveStore 每次都会置 _mirrorPending，"
                 "而「内容与上次已发送相同则跳过」只在「内容来自槽」时生效 → "
                 "正常保存路径上的重复内容永远会重发（被守卫拒绝的整库会被反复重发）。",
         "判据提到「看来源之前」。E2E 用例2 断言第二次相同保存不增加发送次数"
         "（实测 _mirrorStats.skippedSame 由 0 → 1）。"],
    ],
    fill_col=2,
    fill_map={"高": HIGH_FILL, "中": MID_FILL, "低": LOW_FILL},
)

DOCS.mkdir(parents=True, exist_ok=True)
wb.save(OUT)
print("已生成:", OUT)
for ws in wb.worksheets:
    print("  - %-20s %d 行" % (ws.title, ws.max_row - 3))